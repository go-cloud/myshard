language: go

go:
  - 1.3
  - tip

before_install:
  - go get github.com/tools/godep
  - go get code.google.com/p/go.tools/cmd/cover
  - go install -race std
  - mysql -e "create database IF NOT EXISTS test;" -uroot
script:
  - godep go test -cover ./go/...
  - godep go test -race ./go/...
